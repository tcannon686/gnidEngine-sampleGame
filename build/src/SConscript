"""
\file SConscript
\brief Builds the game
"""

Import('env')

game = env.Program(
        target = '#/game',
        source = Glob('*.cpp'),
        LIBS = ['gnid', 'glfw', 'dl'],
        LIBPATH = '#/deps/gnidEngine')

Return('game')

